using Newtonsoft.Json;
using System.Collections.Generic;

/*
 * Index
 * 1. (29) ClassCharacter
 * 4. (47) ClassRoot (Lista de favoritos)
 * 5. (63) Modelo de la lista de favoritos
 */

/*
 * Metodo de empleo
 * 1. Comprobar la url y extraer los atributos de la API (id, nombre y tipo, level, desscripcion etc.)
 * 2. Comprobar si la clase tiene objetos extras tipo digimon (id y name), level (id y level) y type (id y type) etc. y crear los objetos (class)
 * 3. Insertar los objetos en la lista de favoritos (FavoriteItemList) del objeto modelo (ClassDigimon)
 * 4. Comprobar si hay un objeto que tiene el objeto modelo dentro o es un array 
 * Ej. JObject:  info {digimon{id, name} page{id}, limit, count}
 * JArray : [{digimons 1{id, name...} digimons 2{id, name...} ]}]
 */

namespace APIStarWarsAPI
{
    public class Models
    {

        // Nombre de la clase, propiedades de la API

        // 1. Clase Digimon (objeto modelo)
        public class ClassCharacter
        {

            [JsonProperty("name")]
            public string Name { get; set; }

            [JsonProperty("height")]
            public int Height { get; set; }

            [JsonProperty("mass")]
            public int Mass { get; set; }
            
            [JsonProperty("gender")]
            public string Gender { get; set; }

        }

        // 2. ClassRoot
        public class ClassRoot
        {
            [JsonProperty("count")]
            public static int Count { get; set; }

            [JsonProperty("next")]
            public static string Next { get; set; }

            [JsonProperty("previous")]
            public static string Previous { get; set; }

            [JsonProperty("results")]
            public static List<ClassCharacter> Results { get; set; }
        }

        // 3. Modelo de la lista de favoritos
        public class FavoriteItemList // *poner atributos del metodo AddToFavoriteList y ShowFavoriteList*
        {
            [JsonProperty("name")]
            public string Name { get; set; }

            [JsonProperty("height")]
            public int Height { get; set; }

            [JsonProperty("mass")]
            public int Mass { get; set; }

            [JsonProperty("gender")]
            public string Gender { get; set; }

        }
    } // Fin de la clase
}
